extends layouts/main-layout.pug

block content 
    main 
        form(action="/admin/add-product", method="post")
            label(for="title") #{docTitle}
            input(type="text", name="title", id="title")
            button(type="submit") Add Product
